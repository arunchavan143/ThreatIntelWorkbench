'use strict';

module.exports = {
  up: async (queryInterface, Sequelize) => {
    // Optional: Seed initial actors or MITRE data
    return Promise.resolve();
  },

  down: async (queryInterface, Sequelize) => {
    return Promise.resolve();
  }
};
